
```bash

# 修改文件配置
supervisorctl update [program_name]

# 仅扫描并识别配置文件的 增、删、改 变化，但不会应用这些更改
# 执行 update 前，务必先运行，确认输出的是您预期的变更
supervisorctl reread

# 根据 reread 的扫描结果，应用配置变更
# 它会移除已不存在于配置中的程序/进程组，并启动或重启新增或配置有变的程序

supervisorctl update

```