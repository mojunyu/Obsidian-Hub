---
title: 广告消耗监控系统设计
date: 2026-03-18
status: 进行中
tags:
  - 需求
  - 监控
  - 广告
  - 架构设计
---

# 广告消耗监控系统设计

## 方案对比

根据「实时掌握广告投放表现」和「及时发现异常」两个核心需求，推荐采用**实时检测方案**。

| 特性 | 方案一：定时运行规则检测 | 方案二：实时检测（推荐） |
| ---- | ------------------------ | ------------------------ |
| **实时性** | 较差，存在「时间真空期」 | 优秀，数据拉取后立即处理 |
| **异常发现** | 滞后，可能导致损失扩大 | 及时，能迅速预警并止损 |
| **数据新鲜度** | 依赖定时任务频率 | 极高，基于最新拉取数据 |
| **资源消耗** | 集中在定时任务执行时 | 分散在每次数据拉取后 |
| **复杂性** | 相对较低 | 相对较高，需要处理实时流 |

---

## 整体架构概览

构建基于事件驱动的监控系统，当新的广告数据入库时，及时进行处理、分析和告警。

```
+-------------------+      +-------------------+      +-------------------+
|                   |      |                   |      |                   |
|   媒体平台API     |<-----|   现有定时脚本    |----->|     MySQL ad_cost |
|                   |      |    (PHP Cron)     |      |                   |
+-------------------+      +-------------------+      +--------+----------+
                                                              |
                                                              | (数据入库)
                                                              V
+-------------------------------------------------------------------------+
|                                                                         |
|                       **新数据触发 / 定时扫描**                         |
|                                                                         |
+-------------------------------------------------------------------------+
                                 |
                                 V
+-------------------+      +-------------------+      +-------------------+
|                   |      |                   |      |                   |
|   数据清洗与聚合   |----->|      消息队列     |----->|     监控规则检测  |
|   (PHP Worker)    |      |       (MQ)        |      |    (PHP Worker)   |
|                   |      |                   |      |                   |
+-------------------+      +-------------------+      +---------+---------+
                                                              |
                                                              | (异常数据)
                                                              V
+-------------------+      +-------------------+      +-------------------+
|                   |      |                   |      |                   |
|      消息队列     |----->|      告警服务     |----->|   通知渠道        |
|       (MQ)        |      |    (PHP Worker)   |      | (Email, SMS, IM)  |
+-------------------+      +-------------------+      +-------------------+
                                                              ^
                                                              | (监控配置)
                                                      +-------+-------+
                                                      |               |
                                                      |    Redis Cache|
                                                      |               |
                                                      +-------+-------+
                                                              ^
                                                              | (监控配置)
                                                      +-------+-------+
                                                      |               |
                                                      |   MySQL       |
                                                      |   监控配置表  |
                                                      |               |
                                                      +---------------+
```

---

## 详细方案

### 1. 监控配置表设计

#### ad_monitor_rules 表结构

```sql
CREATE TABLE `ad_monitor_rules` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `rule_name` VARCHAR(100) NOT NULL COMMENT '规则名称',
  `project_id` INT UNSIGNED DEFAULT NULL COMMENT '关联游戏ID (可为NULL，表示全局规则)',
  `appid` INT UNSIGNED DEFAULT NULL COMMENT '关联APPID (可为NULL)',
  `os` TINYINT UNSIGNED DEFAULT NULL COMMENT '操作系统 (可为NULL)',
  `country_code` VARCHAR(64) DEFAULT NULL COMMENT '国家编码 (可为NULL)',
  `partner_id` INT UNSIGNED DEFAULT NULL COMMENT '广告渠道ID (可为NULL)',
  `metric` VARCHAR(50) NOT NULL COMMENT '监控指标 (如: cost, click, install, cpa, roi)',
  `monitor_type` ENUM('threshold', 'fluctuation', 'comparison') NOT NULL COMMENT '监控类型: 阈值, 波动, 对比',
  `threshold_value` DECIMAL(10,2) DEFAULT NULL COMMENT '阈值 (如: cost > 1000)',
  `threshold_operator` ENUM('>', '<', '>=', '<=', '=') DEFAULT NULL COMMENT '阈值操作符',
  `fluctuation_percent` DECIMAL(5,2) DEFAULT NULL COMMENT '波动百分比 (如: 20表示20%波动)',
  `fluctuation_period` INT DEFAULT NULL COMMENT '波动周期 (分钟/小时/天, 结合cron)',
  `comparison_metric` VARCHAR(50) DEFAULT NULL COMMENT '对比指标 (如: ROI)',
  `comparison_operator` ENUM('>', '<', '>=', '<=', '=') DEFAULT NULL COMMENT '对比操作符',
  `comparison_value` DECIMAL(10,2) DEFAULT NULL COMMENT '对比值',
  `comparison_dimension` VARCHAR(100) DEFAULT NULL COMMENT '对比维度 (如: campaignid, adgroupid)',
  `status` TINYINT(1) NOT NULL DEFAULT '1' COMMENT '规则状态: 1-启用, 0-禁用',
  `alert_level` ENUM('info', 'warning', 'critical') NOT NULL DEFAULT 'warning' COMMENT '告警级别',
  `alert_channels` VARCHAR(255) NOT NULL DEFAULT 'email' COMMENT '告警通知渠道 (逗号分隔)',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  INDEX `idx_project_id` (`project_id`),
  INDEX `idx_metric` (`metric`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### ad_monitor_alerts_log 表结构

```sql
CREATE TABLE `ad_monitor_alerts_log` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `rule_id` INT UNSIGNED NOT NULL COMMENT '关联的规则ID',
  `alert_level` ENUM('info', 'warning', 'critical') NOT NULL,
  `alert_message` TEXT NOT NULL COMMENT '告警消息',
  `alert_data` JSON DEFAULT NULL COMMENT '触发告警的原始数据或聚合数据',
  `is_resolved` TINYINT(1) NOT NULL DEFAULT '0' COMMENT '是否已解决: 1-是, 0-否',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  INDEX `idx_rule_id` (`rule_id`),
  INDEX `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

> [!TIP] 配置说明
> - **metric**: 监控指标，如 `cost`, `click`, `install`, `convert`, `exposure`，以及衍生指标 CPA、ROI
> - **monitor_type**:
>   - `threshold`: 简单阈值告警，如 `cost > 1000`
>   - `fluctuation`: 波动告警，与前一个周期相比变化超过一定百分比
>   - `comparison`: 对比告警，如不同 campaignid 之间的 CPA 差异过大
> - **维度字段**: `project_id`, `appid`, `os`, `country_code`, `partner_id` 等，可为 NULL 表示全局规则

---

### 2. 数据清洗与聚合模块 (PHP Worker)

负责从 `ad_cost` 表提取最新数据，进行清洗、转换和聚合，然后发送到消息队列。

#### 数据拉取触发方式

| 方案 | 说明 | 推荐度 |
| ---- | ---- | ------ |
| **方案 A** | 修改现有脚本，数据写入后立即触发事件 | ⭐⭐⭐ 推荐 |
| **方案 B** | 独立 Cron Job，定时扫描新增/更新数据 | ⭐⭐ |

#### 数据聚合维度

- **维度**: `project_id`, `appid`, `os`, `country_code`, `partner_id`, `campaignid`, `adgroupid`
- **时间粒度**: 小时、半小时、15分钟
- **衍生指标**: CPA (`cost/install`), ROI (`charge/cost`), CTR (`click/exposure`)

---

### 3. 监控规则检测模块 (PHP Worker)

常驻进程，从 MQ 消费聚合数据，根据预设规则进行检测。

#### 核心流程

1. **消费 MQ 消息**: 监听 `ad_metrics_aggregated` 队列
2. **加载监控规则**: 从 Redis 缓存获取规则（减少数据库查询）
3. **规则匹配与检测**:
   - 维度匹配：优先匹配具体维度，无则匹配通用规则
   - 执行检测逻辑：阈值检测、波动检测、对比检测
4. **发送告警消息**: 检测到异常后发送到 `ad_monitor_alerts` 队列

---

### 4. 异常告警机制 (PHP Worker)

从告警 MQ 消费消息，根据配置的告警渠道发送通知。

#### 支持的通知渠道

| 渠道 | 实现方式 |
| ---- | -------- |
| 邮件 | PHPMailer / Symfony Mailer |
| 短信 | 第三方短信服务 API |
| 即时通讯 | 企业微信/钉钉/飞书群机器人 API |
| 工单系统 | 自动创建工单 |

---

### 5. 技术栈整合

| 组件 | 用途 |
| ---- | ---- |
| **PHP** | 数据清洗、聚合、规则检测、告警发送的 Worker 脚本 |
| **MySQL** | `ad_cost` 原始数据、`ad_monitor_rules` 规则配置、`ad_monitor_alerts_log` 告警历史 |
| **Redis** | 监控规则缓存、历史数据存储（波动检测）、告警限流 |
| **MQ** | 数据聚合队列、告警队列（RabbitMQ / Kafka） |
| **Supervisor** | 管理和守护 PHP Worker 进程 |

---

### 6. 可视化与操作面板（可选）

> [!TIP] 建议开发
> - **规则配置**: CRUD `ad_monitor_rules`
> - **告警列表**: 展示 `ad_monitor_alerts_log`，支持筛选、搜索、标记已解决
> - **实时仪表盘**: 展示关键指标趋势图，集成告警信息
> - **告警通知配置**: 配置不同告警级别的通知人、通知渠道

---

## 实施步骤

1. **数据库设计**: 创建 `ad_monitor_rules` 和 `ad_monitor_alerts_log` 表
2. **改造现有脚本**: 入库后向 MQ 发送消息，或新建 Cron Job 定时扫描
3. **开发监控规则检测 Worker**: 消费 MQ、加载规则、执行检测、发送告警
4. **开发告警服务 Worker**: 消费告警队列、发送通知、记录日志
5. **开发管理后台**: 规则配置、告警查看、通知管理
6. **测试与优化**: 充分测试各种规则和异常情况，调整参数
